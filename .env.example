PORT=5000
HOST=0.0.0.0
DEFAULT_AI=claude
DEFAULT_MODE=incognito

# ── Claude ─────────────────────────────────────────────────
# Only set if auto-detect fails. To find your App ID run:
#   Get-StartApps | Where-Object { $_.Name -like "*Claude*" }
# CLAUDE_APP_ID=Claude_pzs8sxrjxfjjc!Claude

# ── ChatGPT ────────────────────────────────────────────────
# Only set if auto-detect fails. To find your App ID run:
#   Get-StartApps | Where-Object { $_.Name -like "*ChatGPT*" }
# CHATGPT_APP_ID=OpenAI.ChatGPT-Desktop_2p2nqsd0c76g0!ChatGPT
